# RGB_2_Thermal
Image to image translation project in which a generative model is created to convert RGB image to a thermal image. We have a paired set of RGB to thermal image used for training a patch GAN model.
